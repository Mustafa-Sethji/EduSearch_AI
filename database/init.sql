CREATE DATABASE IF NOT EXISTS edusearch CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE edusearch;

-- Tables are auto-created by Sequelize sync on server startup.
-- This script ensures the database exists for Docker MySQL init.
